machine:
  environment:
    NOKOGIRI_USE_SYSTEM_LIBRARIES: true # speeds up installation of html-proofer

dependencies:
  post:
    - bundle exec jekyll build

# This is a good idea, but it also points out lots of issues...sigh!
test:
  post:
    - echo "Looks Good!"
#    - bundle exec htmlproofer ./_site --allow-hash-href --check-favicon --disable-external

general:
  artifacts:
    - "_site" # So we can see the generated files in CircleCI